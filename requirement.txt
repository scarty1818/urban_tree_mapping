[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/USER/REPO/blob/main/notebooks/urban_tree_pipeline.ipynb)

## ⚡ Quickstart (Google Colab, GPU)

1) Click the **Open in Colab** badge above  
2) In Colab: Runtime ▸ Change runtime type ▸ **GPU**  
3) Run the setup cell:

```python
# Clean, reproducible environment
%pip -q uninstall -y ultralytics
%pip -q install --no-cache-dir -r https://raw.githubusercontent.com/USER/REPO/main/requirements.txt

import os
os.environ["ULTRALYTICS_HUB"] = "0"         # avoid Hub auto-login/imports on Colab
os.environ["KMP_DUPLICATE_LIB_OK"] = "TRUE" # quiet MKL dup warnings

import torch
print("CUDA available:", torch.cuda.is_available())


---

# 📦 `requirements.txt`

Create this file at the repo root:

```txt
# Core DL
ultralytics>=8.3,<9
torch>=2.1,<3
torchvision>=0.16,<0.20

# Geospatial stack
rasterio>=1.3
shapely>=2.0
geopandas>=0.14
fiona>=1.9
pyproj>=3.6
rtree>=1.2

# Esri Feature Service access
arcgis>=2.3

# Utils & I/O
opencv-python-headless>=4.9
pillow>=10.0
numpy>=1.26
pandas>=2.2
ruamel.yaml>=0.18

# Optional: progress, plots
tqdm>=4.66
matplotlib>=3.8

